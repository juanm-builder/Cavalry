# Draft Domain

`draft-lifecycle.js` defines the shared lifecycle, normalization, status, grouping, source-reference, and resolution rules used by review workflows and Advisor integrations.

The module is provider-neutral and may not import Advisor application or domain code. Advisor-specific transaction interpretation remains in `@cavalry/advisor`.
