# @cavalry/sync-foundation

Local-only sync types, change tracking, conflict checks, and readiness reporting. Network and production cloud behavior remain intentionally disabled.

The package has no runtime dependency on the Mac app or network adapters. Its pure contract suite runs with `npm test --workspace @cavalry/sync-foundation`.
