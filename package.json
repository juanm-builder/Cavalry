{
  "name": "cavalry-workspace",
  "version": "1.0.19",
  "description": "Cavalry local-first desktop finance app and supporting workspaces.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juanm-builder/Cavalry.git"
  },
  "bugs": {
    "url": "https://github.com/juanm-builder/Cavalry/issues"
  },
  "homepage": "https://github.com/juanm-builder/Cavalry#readme",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @cavalry/mac",
    "build": "npm run build --workspace @cavalry/mac",
    "format": "prettier --write .",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm run typecheck --workspace @cavalry/mac",
    "test": "vitest run --config vitest.config.mjs && npm test --workspace @cavalry/finance-core && npm test --workspace @cavalry/action-review && npm test --workspace @cavalry/advisor && npm test --workspace @cavalry/companion-api && npm test --workspace @cavalry/sync-foundation && npm test --workspace @cavalry/mac",
    "test:integration": "npm run test:integration --workspace @cavalry/companion-api && npm run test:integration --workspace @cavalry/mac",
    "test:e2e": "npm run test:e2e --workspace @cavalry/mac",
    "check": "prettier --check . && npm run licenses:runtime:check && npm run lint && npm run typecheck && npm run build && npm run test && npm run openapi:validate --workspace @cavalry/companion-api && npm run openapi:action-sanity --workspace @cavalry/companion-api && npm run gpt-instructions:check --workspace @cavalry/companion-api && npm run gpt-checkpointed-instructions:check --workspace @cavalry/companion-api",
    "package:mac": "npm run dist:mac --workspace @cavalry/mac",
    "package:mac:intel": "npm run dist:mac:intel --workspace @cavalry/mac",
    "package:release:mac": "npm run dist:release:mac --workspace @cavalry/mac",
    "package:release:windows": "npm run dist:release:windows --workspace @cavalry/mac",
    "licenses:runtime": "node tools/release/generate-runtime-licenses.mjs",
    "licenses:runtime:check": "node tools/release/generate-runtime-licenses.mjs --check",
    "release:validate": "node tools/release/validate-release.mjs",
    "release:security": "node tools/release/security-check.mjs"
  },
  "devDependencies": {
    "@testing-library/react": "^16.0.0",
    "@testing-library/user-event": "^14.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.0.0",
    "globals": "^16.0.0",
    "jsdom": "^26.0.0",
    "prettier": "^3.9.6",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2",
    "vite": "7.3.6",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
